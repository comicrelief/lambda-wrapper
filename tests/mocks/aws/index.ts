import { Context } from '@/src';
import context from '@/tests/mocks/aws/context.json';
import event from '@/tests/mocks/aws/event.json';

export const mockContext = context as Context;
export const mockEvent = event;
